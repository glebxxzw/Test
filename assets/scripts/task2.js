'use strict';

//Задача на превышение скорости автомобиля

function getRandomInt(max) {
  return Math.floor(Math.random() * max);
}

const speedsArray = [];

function getCardata() {
    const obj = {
        number: "", 
        speed: 0
    }; 

    

    return obj;
}


